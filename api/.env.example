# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=drowven
DB_USER=drowven_user
DB_PASSWORD=drowven_password

# (Docker overrides these automatically via docker-compose)
